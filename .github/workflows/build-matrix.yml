name: Build
on: 
    workflow_dispatch:

jobs:
    build:
      runs-on: ubuntu-latest
      strategy:
        matrix:
          version: [ [1.20.4, 1-beta], [1.20.3, 1-beta] ]
      steps:
        -   name: Test
            run: echo ${{ matrix.version[0] }}
        -   name: Test2
            run: echo ${{ matrix.version[1] }}
